name: layer-storpool-repo-add
summary: Add the APT repository for the StorPool MAAS-related packages
maintainer: StorPool <support@storpool.com>
description: |
  This layer adds an APT repository and its public key and provides some
  utility functions for installing StorPool packages on the host machine.
